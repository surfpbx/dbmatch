[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "db-ogre-match"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "ase",
    "tqdm",
]

[tool.setuptools]
packages = ["db_ogre_match"]
package-dir = {"db_ogre_match" = "."}
